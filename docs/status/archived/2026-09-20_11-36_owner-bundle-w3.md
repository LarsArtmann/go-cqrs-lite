> **RESOLVED-BY-ROUTING — docs-health 10th pass (2026-09-21):** all questions answered and applied 2026-09-20/21 — receipts in [TODO_LIST.md](../../TODO_LIST.md) ("Owner decisions — W3 bundle" section, all rows struck) and the M01 row of the 14:12 report. The single open remainder (Q2 iroh P99) is tracked [BLOCKED] in TODO_LIST (Release/Tagging). Archived with per-question resolutions inline.

# Owner Bundle W3 — Five Decisions Requested (2026-09-20 11:36)

> Session: publish-and-prove continuation (SUPERB plan). Everything below is a
> question ONLY the owner can settle; each carries context, options, and the
> session's recommendation. No action taken on any of them.

## Standing questions (carried from earlier waves)

### 1. `readme_claims_test.go` ownership

> **RESOLVED 2026-09-20:** owner delegated ("do what makes the most sense") → kept as a tracked gate; ownership note added to the module-map row.

A foreign test file (`docs/` tree, exercises README claims mechanically),
verified green in T16 and re-verified in this session's api-stability runs.
Nobody has claimed authorship; the auto-commit daemon absorbed it during the
W2 wave. **Question:** keep it as a tracked gate (and under whose name in the
module map), or remove it as an unowned artifact?

**Recommendation:** keep — it is green, load-bearing (README truth), and
deletion of a passing gate is the wrong default for a library repo.

### 2. iroh P99 threshold 50ms → 150ms — keep or revisit?

> **STILL OPEN (owner call)** — tracked [BLOCKED] in TODO_LIST, Release/Tagging: "Ratify one shipped judgment call". The nightly quiet-window probe idea is unexecuted.

The irohengine latency assertions were relaxed from 50ms to 150ms P99 during
the wave (fleet load made 50ms flaky on this host). Under quiet conditions
the engine may well meet 50ms again. **Question:** is 150ms the honest
contract for a p2p transport engine, or should we restore 50ms with a
load-aware guard?

**Recommendation:** keep 150ms for CI honesty, add a nightly quiet-window
probe that reports the actual P99 distribution for a month before tightening.

## New questions (from this session's events)

### 3. Ratify the 10:31 concurrent-corruption repair ruling

> **RATIFIED 2026-09-20** — both the restored 1.27.1 contract and the kept markdown reformats stand (TODO W3 section, row struck).

At 10:31:40–10:53 an unidentified editor downgraded `go 1.27.1` → `go 1.27`
across go.work + 94 module go.mod files (root go.mod left at 1.27.1 — an
inconsistent workspace), set golangci `run.go: 1.26.7`, re-added the removed
`goexperiment.jsonv2` build tag, and deleted the depguard allow-list
(~119 lines). Markdown table reformats rode along in the same edit window
(formatter-consistent). The repair reverted the go/golangci hunks (restoring
the 1.27.1 contract) and KEPT the markdown reformats.

**Question:** ratify (a) the restored 1.27.1 toolchain contract as canonical,
and (b) the keep-the-markdown-reformats judgment?

**Recommendation:** ratify both. The 1.27.1 contract is what the 92-tag wave
was cut against; the markdown hunks are formatter-consistent improvements.

### 4. flake.nix go pin vs nixpkgs lag

> **RESOLVED 2026-09-20 (option c + the gate):** env chain documented as the contract and mechanically enforced by `scripts/check-go-version.sh` (flake app `#check-go-version`, in the `#verify` head + nightly).

`flake.nix:95` says `goToolchain = pkgs.go_1_27` which host nixpkgs resolves
to **1.27.0**, while the repo contract is **1.27.1**. Every run works only
because environments export `GOTOOLCHAIN=auto` (runtime fetch of 1.27.1).
This is a latent trap: any invocation without the env chain silently builds
with 1.27.0.

**Options:** (a) explicit `go_1_27_1` attribute once nixpkgs carries it,
(b) pin via `builtins.fetchurl` of the official tarball now, (c) document
the env chain as the contract and wait for nixpkgs.

**Recommendation:** (c) now + a `check-go-version` gate that fails when host
nixpkgs lag changes (the trap becomes loud instead of silent); move to (a)
when nixpkgs lands 1.27.1.

### 5. Repo edit-lock (flock) during verify windows

> **RESOLVED 2026-09-20 (yes, advisory):** `scripts/lib/verify-lock.sh` (machine-level flock, holder-PID diagnosis, `VERIFY_LOCK=0` opt-out) taken by `#verify`, pre-commit, and the release scripts on write windows.

The 10:31 corruption happened MID-VERIFY (attempt 4 died at lint because of
it, not lint debt). The perpetrator was never identified (crush-daily
scheduler innocent — its windows are 00:30–04:45; the 6 sibling crush
sessions work other repos). **Question:** add a verify-window advisory lock
(`scripts/lib/verify-lock.sh`: flock on `./.verify.lock`, taken by the verify
app, checked by release scripts and a pre-commit hook) so concurrent editors
fail loudly instead of corrupting?

**Recommendation:** yes, advisory-only in v4.x (no behavior change for
consumers; repo-local tooling only). The 2026-09-20 evidence shows the
failure mode is real and costs ~45 min per incident.

---

_Evidence trail: `/tmp/verify-attempt4.log` (corruption timeline),
`docs/status/2026-09-20_10-56_status-report-verify5-live-concurrent-corruption-repaired.md`
(forensics), `docs/agents/gotchas-tooling-build.md:42` (GOTOOLCHAIN noise)._
