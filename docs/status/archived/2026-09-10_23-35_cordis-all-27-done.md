# Status: Cordis Pareto Execution — ALL 27 tasks done (Waves 0–3 complete)

> **RESOLVED (docs-health pass 2026-09-11):** **Superseded — archived by the docs-health pass 2026-09-11.** The §d4 TODO harvest happened (TODO_LIST Cordis section holds exactly the listed follow-ups); the 2026-09-10 §e/Q items were decided in `2026-09-11_00-04` (verify-fast GREEN, M-26 internal-only held).
> Open work lives in [`TODO_LIST.md`](../../TODO_LIST.md); shipped surface in [CHANGELOG.md](../../CHANGELOG.md) `[Unreleased]`.

> **When:** 2026-09-10 23:35 · **Session:** continued "READ, UNDERSTAND, RESEARCH, REFLECT / keep going until done" from the 22:43 break at M-15
> **Input:** [`docs/planning/2026-09-10_08-10_SUPERB-cordis-paradigm-pareto-execution.md`](../planning/2026-09-10_08-10_SUPERB-cordis-paradigm-pareto-execution.md) (M-15..M-27 + housekeeping)
> **Prior state:** Wave 0 + Wave 1 + M-11..M-14 done ([`2026-09-10_22-43_cordis-wave1-done-wave2-at-m15.md`](2026-09-10_22-43_cordis-wave1-done-wave2-at-m15.md))

---

## a) DONE this session (each with its verify gate)

### M-15 — cqrs-lint E018 `projection-without-emitter`

- The mirror of E006: flags projections subscribing to an event type no decider emits and the catalog does not declare — the `user.creted` typo class. **Warning severity** (warn-first guardrail), medium confidence; `catalog.Event` counts as provided (external events); silent when zero emissions detected (scanner limit guard).
- Registered in all five places (detector, RegisterAll, catalog metadata, README table + counts 205→206, RULES.md regenerated); pinned detector count meta-test updated; 6 fixture tests; full self-suite green; lint 0.

### M-16 — catalog coeffect validation in the EventCatalog export

- `catalog.Catalog.ValidateCoeffects` (dangling events = violations; explicit `Producers` honored; unconsumed = advisory, not violations) + `Catalog.DeriveProducersConsumers` exported (the exporter's derivation, now shared; eventcatalog keeps a forwarder).
- Exporter writes `coeffects.md` next to the rendered site (graph table + DANGLING/advisory status + dangling count). 7 new tests. **`#check-eventcatalog` GREEN end-to-end** (npm install + `eventcatalog build`, 54 pages, clean).

### M-17/M-18/M-19 — ground-truth pass

- ADRs 0114/0123/0124/0126/0127 verified against the mapping doc's claims. One correction via addendum: "ADR-0124 §11" — the ADR has no numbered sections; semantics live in code + the layout-planning model doc (verified at `relayout.go:33,64,171`).
- arXiv 2608.25512 PDF archived (`cordis-arxiv-2608.25512.pdf`); grounding addendum quotes the real definitions (twisted composition monoid Def 1, effect context Def 2, coeffect context Def 19, tests/≃ₖ Defs 31–33 + Lemma 32, recovery Theorem 7). M-14's test is the operational form of the paper's test-based equivalence.
- Figures recounted: **84 modules** (was 82), **72 production + 33 test DeferClose sites** (was 47+17). AGENTS.md fixed; mapping-doc addendum records the drift.

### M-21/M-22/M-23 — health-driven engine deactivation (ADR-0137)

- ADR-0137 written + indexed (both READMEs, verify-docs 136/136).
- Implemented: per-engine health tracker (`metaengine/engine_health.go`) — errorfamily Infrastructure/Transient failures count (default 3, `SetEngineFailureThreshold`); quarantine excludes from `routableLocked` (never re-planned onto) and reroutes execution via `effectiveQueryLocked`/`routedQuery` (plan untouched); `StartAutoReprobe` reactivates engines whose `Prober` answers; `ReactivateEngine` for the probe-less. `EngineStats.Health` + `Doctor` "--- Engine Health ---" section surface it all.
- 5 tests under `-race`: quarantine→reroute→stats/Doctor→reactivate roundtrip; Rejection-never-quarantines; no-candidate-fails-loudly; auto-reprobe; threshold override. Full metaengine suite + lint 0.

### M-24/M-25 — equivalence tooling

- `scenario.Interleaved` (the composite, graduated from M-14's test-local type) + `scenario.AssertObservationalEquivalence` + `EquivalenceProbe` + minimal `ScenarioReporter` interface (Helper+Fatalf — rapid.T satisfies it; full testing.TB does not, missing ArtifactDir). `GivenProjection` widened `*testing.T`→`testing.TB` (source-compatible).
- Rapid property test: randomized task/audit interleavings (0–64 events, closed key space for update collisions) — A's answers identical across every key. rapid promoted to direct test-only dep.

### M-26/M-27 — vocabulary + diagram

- M-26: keep-internal default executed and VERIFIED — no Cordis/spatiotemporal/revertible-effect/coeffect-paradigm vocabulary in SKILL.md, README.md, skill references, or DOMAIN_LANGUAGE.md; "coeffect" appears publicly only as shipped API names. Revisit at the v5 gate.
- M-27: Mermaid concept map appended to the mapping doc (Cordis concepts → shipped mechanisms, declined-unified-context edge included).

### M-20 — consolidated release hygiene

- CHANGELOG `[Unreleased]`: five new entries (coeffect gate, E018, catalog coeffect validation, ADR-0137 deactivation, scenario equivalence tooling) — symbol gate green (18 citations).
- API golden regen ×3 this session: 6752 → 6755 (catalog) → 6766 (metaengine health) → **6773** (scenario equivalence); TestEvery green each time.
- doc-check green (1038 → 1042 refs after core.md §3.9 recipe; AGENTS.md contracts #23/#24 added).
- `go work sync` + workspace-sync check OK (rapid dep).

## b) Fixed en route (pre-existing, gate-blocking)

1. **`cmd/cqrs-upgrade` parallel-test race** — `TestPlanUpgrades_StubbedResolver` and `TestPlanUpgrades_CeilingClampsWithoutDowngrade` both swap the package-global `versionResolver` while `t.Parallel()`. Serialized both (comments explain). 10× `-race` green. This is what failed the first verify-fast run.
2. **Layer-check gap from M-14** — scenario's test-only `metaengine/projectionadapter` require lacked an `EXCEPTIONS[scenario]` entry (the script cannot see test-only vs production requires). Exception added with rationale; `check-module-layers.sh` green.
3. **`maintidx` on `Doctor`** — my health section pushed cyclomatic complexity over; extracted `doctorEngineHealthSection()`.

## c) Flakes seen once each (watch list, NOT re-sighted in the final verify)

- `TestTursoEncryption_RoundTrip/aes256gcm` under parallel load (prior session) — did NOT recur.
- `catalog/schema TestEmbedded_SchemaCoversWireKeys` under full-module parallel load — passed 3/3 in isolation. One sighting each; no TODO_LIST entries per the two-sightings rule.

## d) TODO_LIST harvested

The Cordis section now records only follow-ups: `sqliteengine.ResetEngine` (🔥 prioritized), EngineResetter on remaining engines, reset capability in Doctor/Stats, fold-write failover for quarantined engines, E018 fold-case coverage, goleak for metaengine/projectionhost, `[Unreleased]`-position tripwire, wire `#check-file-size` (exporter.go is over 350), release-train note for the projectionadapter replace.

## e) Decisions taken autonomously (per plan §0 defaults, all reversible)

1. **M-15 severity = Warning** (new hard-error rule would break consumers' lint CI; runtime gate remains the hard tier).
2. **M-26 = internal-only until v5** (plan default; verified leak-free).
3. **sqliteengine.ResetEngine = follow-up after M-27** (risk surface; TODO_LIST 🔥).

— Working tree auto-committed by the daemon; nothing pushed.
