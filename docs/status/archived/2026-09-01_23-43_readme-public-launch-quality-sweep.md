# Status Report — README Public-Launch Quality Sweep + GitHub Metadata

**Session:** 2026-09-01, ~21:00–23:43 CEST · **Report written:** 2026-09-01 23:43 CEST
**Scope of this session:** (1) GitHub repo description + topics, (2) root README overhaul, (3) all 72 module READMEs audited and repaired.
**Explicit format note:** the status-report skill's canonical output is a styled HTML dashboard; the user explicitly requested `.md` at `docs/status/`, so this report is Markdown (user instruction wins, per the skill's own override rule).
**Concurrent session:** a parallel workstream (calibration/CI-drift, release-tag repair, issue-#20 CI-failure ledger) has in-flight changes in the shared tree. None of their files were touched. Their commits (`787280c96`, `cb735eb85`, `eed4cf0be`, `1b994fb0d`, status doc `2026-09-01_23-09_*`) are interleaved with this session's daemon commits.

---

## Self-Critique First (asked directly: what did I forget, what could be better?)

**What I forgot / left unverified (carried over from the old README without re-checking):**

1. **"Import `event` alone (3 dependencies)"** — I never counted `event/go.mod` deps myself. Copied from the old README.
2. **Coverage percentages in Maturity** ("event 88%, decider 97%, id 86%, dispatcher 82%") — not re-verified against `nix run .#check-coverage`.
3. **"80+ modules on `/v4` import paths"** — safe number, but I did not count modules myself this session.
4. **Call arity in the watermill Redis example** — doc-check verifies _symbol existence_, not signatures. `watermill.WithBackend(pub, sub, client)` / `WithCommandBackend(pub, sub, client)` (3 args each) were kept from the old README without reading the actual Go signatures.
5. **Badge URL resolution** — I did not fetch the pkg.go.dev badge URL or the shields license badge to confirm they render.
6. **`example/*` and `docs/*` READMEs were excluded** from both the doc-check sweep and the dead-link sweep (my `find` had `-not -path "./example/*"` and `./docs/*`). That is 4 example READMEs + ~24 docs READMEs unaudited, including `docs/README.md` and `docs/status/README.md`.

**What I could have done better:**

7. I ran my sweeps with ad-hoc shell one-liners. The broken-import grep had a regex bug on the first pass (only caught single-segment paths, missed `storage/turso`) — doc-check caught what my grep missed. The lesson: the _gated tool_ beats the ad-hoc grep; I should have gone to doc-check immediately instead of grepping first.
8. I initially misread `storage/README.md:169` (`import ".../turso/v4"`) as correct because it had `/v4` — the module is `storage/turso/v4`. Had I run doc-check first, that false-pass would never have happened.
9. The dead-link checker produced obvious false positives on the first run (code blocks parsed as links); a second, stricter pass was needed. A purpose-built script would not have needed two passes.
10. I audited against the website-launch README template but **deviated from it without recording the deviation in the README itself** (3 badges instead of the template's 4 — Go Report Card skipped because the root module contains only `doc.go`). The reasoning lives only in this report and the session transcript.
11. Did not sweep the repo for the _exact phrase_ the user rejected ("82 independently-versioned" / "82 modules") outside the root README — FEATURES.md/SKILL.md may still carry the precise count the user dislikes.

**What I deliberately did NOT do (and why):**

12. **No commit.** Critical rule: never commit unless the user says "commit" — plus the tree contains another session's uncommitted work (101 dirty files), so a commit now would risk entangling their diff. The auto-commit daemon already swept the first wave (`e69049a07`); my 4 latest README fixes + CHANGELOG remain uncommitted for it.
13. **No homepage URL** on the GitHub repo — no website exists, and pkg.go.dev hides docs under the intentionally-proprietary license; pointing there would be a broken promise until website-launch.
14. **No full `nix run .#verify`** — my diff is `.md`-only; the scoped gates that CAN be affected (doc-check, changelog-symbols) were run and are green. Running `#verify` now would gate the _other session's_ in-flight code changes, muddying attribution of any failure.

---

## a) FULLY DONE

| #  | Item                                                                                                                                                                                                                                                                                                                                                                                                                                     | Evidence                                                                |
| -- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| 1  | GitHub description set (256→265 chars, "82" removed after user feedback, tagline mirrors README)                                                                                                                                                                                                                                                                                                                                         | `gh repo view` verified live; no exact module count per user preference |
| 2  | GitHub topics set: 17 topics, ecosystem-grounded (eventhorizon/EventStore vocabulary), both spelling variants (`event-sourcing`/`eventsourcing`, `event-store`/`eventstore`)                                                                                                                                                                                                                                                             | `gh repo view` — `TOPICS(17)` verified                                  |
| 3  | Root README rebuilt to template: centered header, 3-badge row (Go Reference/CI/License), doc-link bar, Why, Who-is-this-for (5 personas), When-NOT-to-use (4 exclusions w/ alternatives), verified comparison table, Key modules, Key dependencies, Maturity, License                                                                                                                                                                    | commit `e69049a07`                                                      |
| 4  | Comparison table rebuilt — old table cited `go-cqrs`/`cqrs-go`, **repos that no longer exist** (404 verified); replaced with verified `looplab/eventhorizon` + `watermill` cells (code-searched: no hmac/encrypt/upcaster in eventhorizon)                                                                                                                                                                                               | `gh api repos/ProntoPro/go-cqrs` → 404; `gh search code` empty          |
| 5  | Unverified "Apache-2.0 planned" license claim REMOVED (ROADMAP's Apache mentions are about FoundationDB, not a release plan); License section now states only what LICENSE says                                                                                                                                                                                                                                                          | ROADMAP.md grep                                                         |
| 6  | Quick Start verified against `example/readme-quickstart/main.go` — `GOWORK=off go build` **BUILD_OK**                                                                                                                                                                                                                                                                                                                                    | build run                                                               |
| 7  | 8 module READMEs: copy-paste-broken import paths fixed (missing `/v4` on signing/multisig, catalog+asyncapi+eventcatalog, listing, storage pebble/turso mentions, irohengine quic)                                                                                                                                                                                                                                                       | commit `e69049a07`                                                      |
| 8  | turso path rot fixed: turso + indexing READMEs used `go-cqrs-lite/turso/v4`; module is `storage/turso/v4` (go.mod authoritative)                                                                                                                                                                                                                                                                                                         | commit + doc-check                                                      |
| 9  | 9 dead links repaired: 6 × `codec/README.md` (module extracted to external `go-codec`, ADR-0128) + 3 × idempotency parent links (core → external `go-idempotency`, `MemoryStore`/`ErrDuplicate` existence verified in that repo)                                                                                                                                                                                                         | relabeled to external repos                                             |
| 10 | 8 doc-check broken symbol references fixed: nonexistent `metaengine.PlanFromSQLite` → real `sqliteengine.NewSQLiteEngineFromDSN` + `metaengine.Plan`; `metaengine.NewSQLiteEngine` → `sqliteengine.NewSQLiteEngine`; nonexistent `storage.EventSchema`/`SQLiteEventSchema` dropped (re-exports verified in `eventstore_aliases.go`); `watermill.NopLogger` + `bbolt.Options` external symbols disambiguated (plain fence / `bolt` alias) | uncommitted (4 files), gated below                                      |
| 11 | **doc-check over all 72 module READMEs: 1123 references valid across 52 packages, ZERO warnings**                                                                                                                                                                                                                                                                                                                                        | `EXIT=0` log `.gotmp/doccheck-all-readmes-2.log`                        |
| 12 | Dead-link sweep over all non-docs READMEs: clean                                                                                                                                                                                                                                                                                                                                                                                         | ad-hoc sweep, re-run after fixes                                        |
| 13 | CHANGELOG `[Unreleased] → Fixed` entry for the whole wave; `check-changelog-symbols.sh` **green** (143 citations verified against API golden)                                                                                                                                                                                                                                                                                            | `EXIT=0`                                                                |
| 14 | Daemon committed wave 1: `e69049a07 docs: overhaul root README and fix broken module import paths across 16 READMEs`                                                                                                                                                                                                                                                                                                                     | git log                                                                 |

## b) PARTIALLY DONE

| # | Item                          | Works                                                                                         | Open                                                                                                                                                          | Effort       |
| - | ----------------------------- | --------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ |
| 1 | Module README quality         | All rot (paths/links/symbols) fixed; zero-warning doc-check                                   | NOT a per-module template pass (no badges/personas on the 60+ module READMEs — arguably wrong scope for module docs, but undecided)                           | S (decision) |
| 2 | Verification of README claims | Code-example APIs now doc-check-verified                                                      | Human-curated claims ("3 dependencies", coverage %, "80+") still unverified                                                                                   | M            |
| 3 | GitHub metadata               | Description + topics done                                                                     | Homepage empty (deliberate, pending website decision); Go Report Card badge deliberately skipped (root module = 1 file); social preview image never discussed | S            |
| 4 | "82" eradication              | Description + root README intro use no count ("80+" kept in Maturity — pre-existing phrasing) | Exact-phrase sweep of FEATURES.md/SKILL.md/AGENTS.md for "82 …modules" not done                                                                               | S            |

## c) NOT STARTED

| # | Item                                                                                                                                                                                                              | Why                                                                        |
| - | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| 1 | example/ README audit (4 files: getting-started, taskmanager, readme-quickstart + stray nested `example/readme-quickstart/readme-quickstart/` dir noticed in `ls`)                                                | Excluded from sweeps; suspected same rot class                             |
| 2 | docs/ README audit (24 files, non-archive: docs/README.md, docs/status/, docs/planning/, docs/benchmarks/)                                                                                                        | Archives excluded on purpose; top-level docs READMEs just never looked at  |
| 3 | Skill-reference consistency check: does `recipes.md`/`modules.md` also reference `PlanFromSQLite`, `EventSchema`, `watermill.NopLogger`-style dead API? Canonical doc-check (SKILL.md set) not run this session   | My diff didn't touch skill files; but the dead-API rot may exist there too |
| 4 | Signature-verification of `WithBackend(pub, sub, client)` / `WithCommandBackend` arity                                                                                                                            | doc-check checks existence only                                            |
| 5 | Metaengine API decision: `PlanFromSQLite` is referenced by `metaengine/dsl.go:17` comment AND was wanted by the old README — add the real convenience API, or fix the comment? (comment rot confirmed, not fixed) | API addition needs golden regen + dep-budget thought → needs user decision |
| 6 | `storage.EventSchema` symmetry: snapshot/checkpoint DDL are re-exported in `eventstore_aliases.go` but events DDL is NOT (exists only as `storage/sql` Dialect methods). Add re-export?                           | API addition → decision                                                    |
| 7 | GitHub About homepage + social preview                                                                                                                                                                            | Blocked on website-launch decision                                         |

## d) TOTALLY FUCKED UP (found this session — all fixed, listed for honesty)

| # | What was broken                                                                                                                                                                                                   | Severity                    | Root cause                                                        | Status                                               |
| - | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------- | ----------------------------------------------------------------- | ---------------------------------------------------- |
| 1 | README comparison table cited `go-cqrs` + `cqrs-go` — **repos that no longer exist** (ProntoPro/go-cqrs → 404). Public repo advertising dead competitors with ✅/❌ scorecards                                    | High (credibility)          | Table written against repos that were deleted/renamed             | FIXED — rebuilt with verified live repos, dated note |
| 2 | 8 module READMEs shipped import paths that **fail on first `go get`/copy-paste** (missing `/v4`; turso wrong top-level path). Public users hit this at install time                                               | High (user-facing breakage) | READMEs written before module restructure, never gated            | FIXED — doc-check green                              |
| 3 | `metaengine.PlanFromSQLite` documented in 2 README fences while **not existing anywhere in Go source** (and `dsl.go:17` comment still references it)                                                              | High (docs lie)             | Convenience API never created or was removed; comment left behind | FIXED in READMEs; comment rot remains (see c5)       |
| 4 | `storage.EventSchema()`/`SQLiteEventSchema()` documented but not exported anywhere reachable                                                                                                                      | Medium                      | Alias file re-exports snapshot/checkpoint but not events DDL      | FIXED in docs; API gap logged (c6)                   |
| 5 | License section promised "Public release under Apache-2.0 is planned (see ROADMAP.md)" — ROADMAP says no such thing                                                                                               | Medium (trust)              | Aspiration written as fact with a citation that doesn't check out | FIXED                                                |
| 6 | Not mine, but noticed and relevant: **11 pre-existing master CI failures** (issue #20, other session's ledger `eed4cf0be`) and the **v4.8.0 tag had to be repaired** (`787280c96`, proxy serving fix `cb735eb85`) | High (project)              | Other session's workstream                                        | NOT MINE — tracked by parallel session               |

## e) WHAT WE SHOULD IMPROVE

1. **READMEs are outside every gate — that is the systemic failure this session exposed.** 8 broken import paths, 9 dead links, 8 broken symbol refs, and a comparison table citing dead repos all lived in READMEs _because_ doc-check's canonical invocation only covers SKILL.md + references + AGENTS.md. Fix: add READMEs to the canonical doc-check file list (flake app / CI step). This single change converts everything I fixed by hand into a mechanically-enforced invariant.
2. **Ad-hoc greps lose to gated tools.** My first-pass import grep had a regex blind spot; doc-check caught what it missed. Rule going forward: run the repo's own gate FIRST, ad-hoc sweeps only as supplement.
3. **Human-curated numbers in READMEs rot silently** (counts, coverage %, dep counts). Either curate harder or add a meta-test asserting README claims against `go.mod`/coverage output.
4. **Dead-link checking should be a script** (`scripts/check-doc-links.sh`), not a session-local shell one-liner that needs two passes to suppress false positives.
5. **Template deviations should be recorded where the deviation lives** (a comment in the README or a docs note), not just in session transcripts.
6. **External-API references in ```go fences need a convention** (this session: plain fence for watermill-redisstream, `bolt` alias for etcd). Write the convention down in CONTRIBUTING or the doc-check README so the next writer doesn't trip the gate.
7. **Concurrent-session hygiene worked** (re-read before CHANGELOG edit, foreign files untouched) — keep doing exactly this; the 101-file dirty tree was handled without a single collision.

## f) TOP 50 NEXT TASKS (ranked by impact; harvest into TODO_LIST/ROADMAP)

| #  | Task                                                                                                                                                                              | Impact   | Effort | Category      |
| -- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------ | ------------- |
| 1  | Add all module READMEs to the canonical doc-check invocation (flake app + CI) so README rot is gated mechanically                                                                 | Critical | S      | Quality       |
| 2  | Run canonical doc-check (SKILL.md + references/*.md + AGENTS.md) and fix anything the README rot-class shares (PlanFromSQLite etc.)                                               | Critical | S      | Documentation |
| 3  | Sweep skill references + FEATURES.md for dead API (`PlanFromSQLite`, `EventSchema`, `watermill.NopLogger`, `bbolt.Options`) and fix                                               | High     | M      | Documentation |
| 4  | Fix `metaengine/dsl.go:17` comment that references nonexistent `PlanFromSQLite`                                                                                                   | High     | S      | Bug           |
| 5  | Verify `watermill.WithBackend(pub, sub, client)` / `WithCommandBackend` call arity against real signatures; correct README if wrong                                               | High     | S      | Bug           |
| 6  | Verify "event alone (3 dependencies)" claim vs `event/go.mod`                                                                                                                     | High     | S      | Documentation |
| 7  | Re-verify Maturity coverage percentages vs `nix run .#check-coverage`; update README                                                                                              | High     | S      | Documentation |
| 8  | Count modules and normalize every "80+"/"82" claim repo-wide (FEATURES.md, SKILL.md, AGENTS.md)                                                                                   | High     | S      | Documentation |
| 9  | Audit `example/*` READMEs with the same sweeps (incl. the stray nested `example/readme-quickstart/readme-quickstart/` dir — investigate what it is)                               | High     | M      | Documentation |
| 10 | Audit non-archive `docs/*` READMEs (docs/README.md, status, planning, benchmarks)                                                                                                 | Medium   | M      | Documentation |
| 11 | Turn the dead-link checker into `scripts/check-doc-links.sh` + wire into `#verify`/CI                                                                                             | High     | M      | Quality       |
| 12 | Add a README-claims meta-test (counts/coverage/dep-numbers asserted vs repo reality)                                                                                              | High     | M      | Quality       |
| 13 | Update `example/readme-quickstart` to non-deprecated `ExecuteRef`/`LoadRef` (gopls: both pair forms deprecated, removed v5) and keep root README in sync                          | High     | S      | Bug           |
| 14 | `go mod tidy` watermill module (gopls: `dedup/v4` imported but unused)                                                                                                            | Medium   | S      | Cleanup       |
| 15 | `go mod tidy` catalog module (gopls: `templ-components/utils` should be direct)                                                                                                   | Medium   | S      | Cleanup       |
| 16 | Decide: add real `metaengine.PlanFromSQLite(dsn, queries...)` convenience (README + comment both want it; needs golden regen)                                                     | Medium   | M      | Feature       |
| 17 | Decide: add `storage.EventSchema`/`SQLiteEventSchema` re-exports for symmetry with snapshot/checkpoint                                                                            | Medium   | S      | Feature       |
| 18 | Verify pkg.go.dev badge URL + shields license badge actually render (fetch both)                                                                                                  | Medium   | S      | Documentation |
| 19 | Decide Go Report Card badge: test what goreportcard shows for root + event module before adding                                                                                   | Low      | S      | Documentation |
| 20 | Record the 3-vs-4 badge deviation in CONTRIBUTING or README (template compliance note)                                                                                            | Low      | S      | Documentation |
| 21 | Set GitHub About homepage when website-launch lands (or decide pkg.go.dev despite hidden docs)                                                                                    | Medium   | S      | Feature       |
| 22 | Upload a repo social preview image (GitHub settings; needs an asset)                                                                                                              | Low      | S      | Feature       |
| 23 | Decide external-API-in-docs convention and write it into CONTRIBUTING/doc-check README                                                                                            | Medium   | S      | Documentation |
| 24 | Consider API-stability golden regen habit-check: this session added zero exported symbols — verify `cmd/api-stability` golden untouched (should be)                               | Low      | S      | Quality       |
| 25 | Full `nix run .#verify` once the parallel session's tree lands (docs-only diff; joint tree currently mid-flight)                                                                  | High     | L      | Quality       |
| 26 | CHANGELOG: consider a `Changed` entry for the root-README public-launch polish (Fixed entry exists; Changed is optional per policy)                                               | Low      | S      | Documentation |
| 27 | Check whether `.github/` has ISSUE_TEMPLATE/PULL_REQUEST_TEMPLATE; add if missing (public repo now)                                                                               | Medium   | S      | Cleanup       |
| 28 | Check GitHub Discussions/wiki settings for the now-public repo; enable/disable deliberately                                                                                       | Low      | S      | Cleanup       |
| 29 | Add `docs/DOMAIN_LANGUAGE.md` link to README if the file exists (didn't check)                                                                                                    | Low      | S      | Documentation |
| 30 | Root README Key-modules table: verify each Purpose line against current module reality (catalog exporters, projectionhost DLQ wording)                                            | Medium   | M      | Documentation |
| 31 | README comparison: consider strict-template blanks instead of `✗` glyph (cosmetic)                                                                                                | Low      | S      | Documentation |
| 32 | Verify the V3/V4 migration-guide link targets still exist and describe current v4 (dead-link sweep passed; content accuracy not checked)                                          | Medium   | M      | Documentation |
| 33 | Add the "Guide (SKILL.md)" link-bar entry rationale to the readme-template reference so future launches keep it                                                                   | Low      | S      | Documentation |
| 34 | Sweep all README code fences for other external-package aliases that shadow cqrs package names (`turso` is in doc-check skip-list; `bbolt`-class collisions may exist elsewhere)  | Medium   | M      | Quality       |
| 35 | Consider pinning doc-check's `isStdlibOrBuiltin` skip-list additions (`redis`, `slog` alias uses) as data, not code                                                               | Low      | S      | Quality       |
| 36 | example/readme-quickstart/README.md — verify its code block matches the (possibly future) non-deprecated main.go                                                                  | Medium   | S      | Documentation |
| 37 | Verify `storage.SQLiteEnableWAL` neighbors in storage/README Quick Start section remain accurate post-fix (doc-check passed symbols; prose claims unchecked)                      | Low      | S      | Documentation |
| 38 | Add GitHub topics sweep script (`gh repo edit --add-topic` from a checked-in file) so metadata is code, not tribal memory                                                         | Low      | S      | Cleanup       |
| 39 | Re-run `gh repo view` after next push to confirm description/topics survived any repo setting changes                                                                             | Low      | S      | Cleanup       |
| 40 | Confirm daemon committed the 4 remaining README fixes + CHANGELOG (currently uncommitted); verify commit boundaries don't mix the parallel session's files                        | High     | S      | Cleanup       |
| 41 | Add a "Docs" entry for ADR index in the README link bar (docs/adr/README.md exists)                                                                                               | Low      | S      | Documentation |
| 42 | Verify `docs/migration/MIGRATION-GUIDE.md` + `V3_MIGRATION.md` content still matches v4 reality                                                                                   | Medium   | M      | Documentation |
| 43 | Consider adding the AGENTS.md public-launch facts (public repo, proprietary license decision) as a README footnote for human visitors                                             | Low      | S      | Documentation |
| 44 | Re-check `metaengine/README.md:362` constructor table: ensure `NewSQLiteEngine(db)` row is attributed to the sqliteengine module after the fix                                    | Low      | S      | Documentation |
| 45 | Run `nix run .#check-arch` after the parallel session's go.mod sweep lands (not mine, but adjacency risk)                                                                         | Medium   | S      | Quality       |
| 46 | Consider harvest of (f) items into TODO_LIST.md via docs-health HARVEST (this section is the input)                                                                               | High     | S      | Cleanup       |
| 47 | Check whether `watermill/README.md` line 73-74 plain fence renders with syntax highlighting loss that matters (external-plugin snippet)                                           | Low      | S      | Documentation |
| 48 | Verify shields.io `license-Proprietary-red` badge link text matches LICENSE naming ("Proprietary" vs "PROPRIETARY LICENSE")                                                       | Low      | S      | Documentation |
| 49 | Sweep for em dashes introduced by my README rewrite (repo rule bans them in source code; README is prose, but user style may prefer none)                                         | Low      | S      | Documentation |
| 50 | Add this session's lesson to AGENTS.md gotchas: "READMEs are ungated — run doc-check with README args after any docs wave" (or better, fix #1 and the gotcha becomes unnecessary) | Medium   | S      | Documentation |

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF (3)

1. **Homepage/website:** should the GitHub About homepage stay empty until the lars.software website-launch, or point at pkg.go.dev now — accepting that pkg.go.dev hides all documentation under the intentionally-proprietary license (so the link lands on a page that shows module lists but no docs)?
2. **`PlanFromSQLite`:** the old README wanted it, the `dsl.go:17` comment still references it, and I removed it from docs because it doesn't exist. Do you want the real convenience API added to `metaengine` (I'll do golden regen + dep-budget check), or should docs/comments just describe the current two-step reality?
3. **README numbers:** are the Maturity statistics (80+ modules, coverage percentages, "3 dependencies") numbers you curate by hand, or do you want the CI meta-test (item #12) that fails the build when README claims drift from repo reality? My recommendation is the meta-test.

---

**Awaiting instructions.** Per the status-report skill this report would normally be committed with a detailed message; skipped because (a) the critical rule requires an explicit "commit", and (b) the tree holds the parallel session's 97 unrelated in-flight files — say the word and I will stage and commit ONLY this report file.
