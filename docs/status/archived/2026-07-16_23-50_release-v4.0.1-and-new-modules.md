# Release Status: v4.0.1 Patch Wave + 3 New Module First Releases

> **Date:** 2026-07-16
> **Tags created:** 22 (3 new modules + 19 patch releases)
> **Commit:** `e1b874fa`

---

## What Was Released

### 3 New Module First Releases

| Module                | Tag                          | Description                                                                   |
| --------------------- | ---------------------------- | ----------------------------------------------------------------------------- |
| `cmd/cqrs-lint`       | `cmd/cqrs-lint/v0.1.0`       | Domain-aware linter: 60 rules, 165 tests, CLI with SARIF/JSON/markdown output |
| `retry`               | `retry/v4.0.0`               | Zero-dep exponential backoff+jitter (`Do`, `Config`, `ErrExhausted`)          |
| `idempotency/kvstore` | `idempotency/kvstore/v4.0.0` | KV-backed idempotency store (pluggable backends)                              |

### 19 Module Patch Releases (v4.0.1)

**Tier A — Bug fixes:**

- `projectionhost/v4.0.1` — stagger-shutdown leak, Status() sort, purgeDLQ init
- `watermill/v4.0.1` — EventBus dispatch deadlock in Close()

**Tier B — Additive features:**

- `storage/v4.0.1` — IS NULL operators, RawWhere, ViewUpdater, BLOB support
- `kv/v4.0.1` — OpIsNull, OpIsNotNull

**Tier C — Metadata normalization:**

- `event/v4.0.1`, `decider/v4.0.1`, `middleware/v4.0.1`, `signing/v4.0.1`,
  `encryption/v4.0.1`, `listing/v4.0.1`, `codec/v4.0.1`, `otel/v4.0.1`,
  `schema/v4.0.1`, `snapshot/v4.0.1`, `id/v4.0.1`, `graph/v4.0.1`,
  `scenario/v4.0.1`, `scheduling/v4.0.1`, `transport/http/v4.0.1`

## Pre-Release Fixes

1. **Critical compile bug fix** — `cmd/cqrs-lint/pkg/rules/api/a011_a014_a017.go`
   had `slices.Contains()` with zero arguments (broken refactor `b3931503`).
   Fixed to `slices.ContainsFunc` with suffix predicate.
2. **release.yml CI coverage** — Expanded from 19 hardcoded modules to auto-discovery
   of all 50 buildable modules from `go.work`. Added missing `GOEXPERIMENT=jsonv2` env.
   Added `*/v*` tag pattern for per-module tags.
3. **CHANGELOG split-brain** — Merged duplicate `[Unreleased]` into `[4.0.0]`.
4. **api-stability** — Added `cmd/cqrs-lint` to tracked modules. Golden file
   regenerated (2217→2223 exports).

## Verification Gates Passed

- `nix run .#build` — exit 0
- `nix run .#test` — 76 packages ok, 0 fail
- `nix run .#lint` — 0 issues across all 52 modules
- `go test -race` on cqrs-lint — 11 packages, 0 races
- All 22 tags verified as annotated, pointing to correct commit
- All 3 new modules build in isolation (`GOWORK=off`)

## Blocked

- **Push to origin** — Tags are created locally but NOT pushed (safety rule:
  never push without explicit user approval). Run `git push origin --tags` when ready.
- **Go proxy verification** — Cannot verify until tags are pushed.
- **GitHub Actions release.yml** — Will trigger on push, creates GitHub Releases.
