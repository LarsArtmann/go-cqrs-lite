# Status Report — Docs-Health 7th Pass COMPLETE: 2026-09-13..16 Wave Harvested, Annotated, Archived; Living Docs Rebuilt

> **When:** 2026-09-16 ~12:05–13:05 CEST · **Mandate:** "View ALL `**/2026-0*` files! Execute the docs-health SKILL! PROPERLY! FUCKING SUPERBLY!!! TODO_LIST/CHANGELOG/AGENTS/README/ROADMAP/FEATURES must be all SUPERB! Archive FULLY done and UPDATED (inline strikethrough) .md files!"
> **Mode:** docs-health AUDIT (BUILD + HARVEST + VERIFY + ANNOTATE + ARCHIVE) over the 83 active `2026-0*` targets + the six living docs.
> **Skill discipline held:** SKILL.md + 4 references (harvest-guide, resolving-items, doc-ownership, health-report-format) loaded BEFORE acting; the 6th-pass report studied for house conventions; exact-match assertions on every scripted edit (which caught 3 of my own bad patterns — see §d).
> **Concurrent session:** a second session was LIVE during this pass (fixed the SKILL.md faq-link class + extended `cmd/doc-check/navlinks.go` at 12:48, landed via daemon `dd85418e3`). Marked where it mattered.

---

## a) FULLY DONE (each verified against the tree/gates this session)

1. **Inventory + read of the active set:** all 91 active+archived-adjacent `2026-0*` files inventoried (1,414 total under `docs/*/archived|archive`); **43 status/planning/review files read** (26 status reports — most in full, six via targeted §b/§c/§f extraction — plus planning/review files classified); living docs read (TODO_LIST 922 lines full, README full, CHANGELOG `[Unreleased]`, FEATURES structure + every targeted row, ROADMAP structure + OQ/release-history sections, AGENTS in context). Honest coverage split: §b1.
2. **VERIFY with mechanical evidence (not report trust):** `find -name go.mod` → **91** (AGENTS claimed 88 — stale); `git tag -l` → `claiming/*` and `queue/*` **absent** (tag rows stay open), `watermill/v4.7.0` absent (TODO claim current), `otel` latest v4.4.0 (`DBSystem` unpublished — the storage-tag hazard is real), `benchkit` v4.5.0 (statistical-rigor APIs untagged); shipped-symbol greps: `commandlifecycle.TypeRejected="command.rejected"` + classification tests, `metaengine/failover.go` `reactivateIfStable` (the TODO's "Close the CatchUpEngine snapshot race" was ALREADY FIXED per CHANGELOG 2026-09-13), `storage/sql/otel.go` dialect helpers ×8, `EncodeVectorF32`, `Deprecated` markers on both `Infer` entry points; wiring greps: queue trio + claiming present in `go.work`, flake `testModules`, and `cmd/api-stability` modules list; CHANGELOG coverage greps for every TODO row I closed (all receipts found).
3. **TODO_LIST truth pass:** **29 `[x]` completed blocks deleted** (policy: done work lives in CHANGELOG; every deletion receipt-checked against CHANGELOG first — erraudit @566, private-deps @357, CatchUpEngine @536, file-size/CI kill-switches/pre-commit gates all present); **5 stale open rows closed** (T17 `command.rejected` 🔥, storage/sql `db.system`, CatchUpEngine snapshot race 🔥, Doctor entry-point counters, sqlstore hardening tails narrowed to their genuinely-open halves); **2 empty sections dropped** (Quick-win batch, Skill-docs navigation — all items DONE 2026-09-15/16); **2 DECIDED-against rationales preserved** by moving them into the Declined guard list (per-engine high-water marks; `CatchUpEngineWithResult`); **7 misfiled CI rows moved** from Code Quality into CI/Infrastructure; **~16 rows harvested in** across 4 new sections (queue-family refresh + tags + live-PG conformance + docs/config tail; benchkit statistical-rigor tail ×3; vector-search verification tail ×3; watermill sibling skill ×2; md-go-validator CI integration ×1; CI +5: config-corruption root-cause 🔥, pre-commit hook hardening, nightly gate cron, `ERRAUDIT_PAT` [BLOCKED], MySQL-VM green suite) + the recipes-gate CI posture row. Result: **0 `[x]` rows, 95 open rows, 809 lines** (recounted at report time). Header pass-list extended to 2026-09-16.
4. **ROADMAP:** Release History `[Unreleased]` refreshed with the full 2026-09-13..16 bullet (command-side W1–W3, reconciliation, claiming + queue family, T17 partition, skill-docs gates + recipes harness, vector-every-engine, benchkit rigor, CI kill-switches, `Infer` deprecation, ADR-0139, fifth config recovery); **MariaDB 11.7 VECTOR claim labeled UNVERIFIED inline** (the 18-32 report's §d1 self-caught overclaim — JS-walled KB, 404'd source paths); **zenoh go/no-go added as Open Question #2** (W1–W3 surfaces + report link — lifts the three reports' harvest WAIT); OQ-14 (command-rejection) marked HALF RESOLVED (T17 option A shipped 2026-09-15; payload-capture question remains); OQ list renumbered 2→15 after the insert.
5. **FEATURES:** command-lifecycle row rewritten (6 event types incl. `command.rejected` + `RejectionLog` partition, `CommandsByActor`); **3 queue-family rows added to the Module Maturity Matrix** (contract + sqlite + postgres, conformance-green receipts); benchkit row updated (88 → 151 tests + `RunRepeated`/`MetricVariation`).
6. **AGENTS.md:** "88 `go.mod`" → **91** (verified by `find`); queue family added to the Tier 4 list; module-count drift unified (82/85/78 → one honest formulation; final form corrected at report time, see §d7).
7. **modules.md:** `commandlifecycle` row "5 lifecycle event types" → **6** with the `TypeRejected`/`RejectionLog` partition note (doc-check-scanned; gate green after).
8. **ANNOTATE — 15 active reports bannered with dated STATUS/RESOLVED-BY-ROUTING banners + inline `~~…~~` strikes carrying receipts (~30 strikes):** the four 09-16 reports (02-09 §f1/2/4/43; 08-04 §f6/7/9/11/42; 08-05 §f2/16; 09-35 §f3), the five 09-15 reports (15-21 superseded, 15-22 §C3, 18-19 §f30, 18-32 §f26, 19-27 §f2, 19-45 routed), and the 09-13 audit wave (08-47 §f17-19/22/24/26 — the productization quintet SHIPPED 09-16; 08-52 resolved-by-routing; 09-17 §f4/5/8/9/10; 12-21 §f11). **3 prepared verdicts overturned by verification before striking** (TestRecipesCompile "still red" → re-ran GREEN; file-size baseline review → resolved 09-15; gci decision → resolved 09-15/16) — the verify-before-strike discipline paid again.
9. **ARCHIVE — 4 files `git mv`'d** to `docs/status/archived/` (cec9248da work record — provenance closed, zero forward-looking items; 08-52 quick-win batch — superseded by 15-21/18-19; 15-21 quick-win tail — superseded by 18-19, its §f1–11 struck with receipts post-move; 19-57 zenoh clarification — harvest done). Inbound links repointed (TODO_LIST, 18-19 report, status README).
10. **docs/status/README.md index:** 7th-pass entry added (harvest/annotate/archive summary + the stay-live list with reasons); the two "active; harvest at the next pass" pointers corrected to their archived paths.
11. **On-sight link fixes:** `recipes.md` ×2 wrong-depth source-file links (`../../../../../` → `../../../../`), `md-go-validator-review.md` wrong-path ADR link (→ `../adr/`); `check-doc-links` went 5 broken → 0.
12. **Gates at close:** `cmd/doc-check` → **✓ 1,142 refs / 49 packages, zero-warning** (run ×3 across the edits; only the 4 pre-existing union notices) · `TestRecipesCompile` → **ok 3.5s** (re-verified live, resolves the 08-05 report's §b4) · `scripts/check-doc-links.sh` → **0 broken / 771 targets** · `go build` of cmd/doc-check (via `go run`) green.
13. **Inline health report delivered:** Accuracy **8.75** / Fitness **9.25** with visible math (post-fix state; prior baseline = 6th pass 9.25/10).

## b) PARTIALLY DONE

1. **Read coverage was not uniform.** Six status files got targeted §b/§c/§f extraction instead of full reads (12-10, 12-16, 14-14, 14-46, 15-55, 17-40); two planning-adjacent files were classified by structure only (15-55 via section list, 17-40 via head). 17-40 stays live deliberately (it is EVIDENCE referenced by the live planning doc + T16 memo, not open work) — but that rationale is recorded only in this session, not in the file.
2. **The "resolve EVERY numbered item" letter is only partially met** on the four 50-item-brainstorm reports (02-09, 08-04, 08-05, 09-35): verified-resolved rows are struck, but their long §f tails are banner-routed to TODO_LIST rather than struck row-by-row (the harvest-guide's anti-dump rule applied; the two-tier convention strike-what-shipped/route-the-brainstorm is MY interpretation, not a ratified skill rule — see §g2).
3. **Harvest depth:** high-value rows routed; ~80 low-priority brainstorm rows deliberately left in their reports (anti-dump). If the user wants the full tails in TODO_LIST, that is a separate sweep.
4. **Five planning docs were classified, not line-verified** (SUPERB publish-reset-v5-train, command-side depth, OTEL observability, to-the-max, vector spike) — their execution reports carry the authoritative status and are bannered; the plans themselves follow the reconcile-procedure but were not re-verified this pass.
5. **FEATURES' two unverifiable guarantee rows** ("`nix run .#lint` passes across all modules", "Race-free") were left as-is — no composed lint/race evidence exists since the 09-16 config recovery, and I did not run them (docs-only mandate + load). Counted as the 2 remaining Medium accuracy findings in the health report.
6. **The 08-42 archived file carries 0 `~~`** — it is a provenance record with zero forward-looking items (the completeness gate's intent is satisfied; its letter is not). Deviation noted in the health report.
7. **Attribution, again:** everything landed via daemon `chore:` commits (I hold the no-unprompted-commits rule); authored history for this pass is only this report. `git status` at close: clean except AGENTS.md (absorbed minutes later).

## c) NOT STARTED (deliberate, mandate-scoped)

1. Composed `nix run .#verify` / `#lint` — not run (docs-only session; the standing [BLOCKED] quiet-window row owns it).
2. The 17 modules missing from `module-map.md` (91 go.mods incl. root = 90 modules vs **73 rows** — census not taken; see §f1).
3. FEATURES Module Maturity Matrix census vs 90 modules (I added the queue rows but did not count the whole matrix).
4. Re-measuring the README/FEATURES coverage claims (86–96%) — untouched.
5. Zenoh 4-layer placement table persistence (19-57 §b1) — still chat-only, correctly per its own scope note.
6. Per-file index for the 09-13 wave in status README (wave-level summary written; the 09-11 batch got the per-file treatment, this wave did not yet).
7. All TODO_LIST work items themselves — this pass routes and verifies; it does not execute the backlog.

## d) TOTALLY FUCKED UP (own failures, no varnish)

1. **"90-module table" — I shipped a made-up count into AGENTS.md and caught it only at report time.** After unifying the 82/85/78 drift I wrote "full 90-module table with internal notes" for module-map.md — reasoning from the go.mod count (91 incl. root) WITHOUT counting the map's rows. The map has **73**. Caught during this report's prep (I ran the count I should have run before editing); AGENTS now honestly says "73 of 90 modules rowed". The 2-minute-late count cost a lie-in-master for ~15 minutes and a 9th edit to AGENTS. The rule I already knew: never write a number you did not compute in the same minute.
2. **Wrong theory, briefly shipped as a TODO row:** when `check-doc-links` flagged the faq anchor while `doc-check` was green, I concluded "slugger false positive", wrote a TODO row to "align the slugger", and my inline health report implied the links were correct. A concurrent session proved the links were GENUINELY broken (wrong relative path from SKILL.md's directory — `faq.md` vs `references/faq.md`) and fixed both the link and doc-check's short-name fallback. I then deleted my wrong row. Root cause: I treated doc-check's green as "the anchor is GitHub-correct" without asking WHERE the link resolves FROM — two tools, two resolution semantics, and I picked the wrong one to trust without testing either's assumptions.
3. **Four failed exact-match edits in a row (19-27, 19-57, 12-21, 15-22)** — I composed `old_string`s from my reading summary instead of extracting the file's exact bytes first; every one failed on whitespace/column-width differences, and each cost an extra `awk` round trip. The lesson is written in three earlier reports and the gotchas file; I re-learned it four times in one hour before switching to extract-then-edit (after which: zero failures).
4. **TODO_LIST CI rows landed in the WRONG SECTION.** I anchored my 4-row insert on the CI-triage row's effort line without noticing that row itself has been misfiled under Code Quality for days. The insert therefore landed in Code Quality; my later `grep '^## '` sanity check caught it, and fixing took 2 more script attempts (one aborted on a wrong block-count assertion — I forgot the moved block includes the CI-triage row itself). Three avoidable round trips from not reading the anchor's surrounding context.
5. **ROADMAP OQ duplicate numbering:** I inserted zenoh as a new "2." without checking the existing list first — creating two "2." items — then needed 13 renumbering edits to fix. Count-before-insert would have been one edit.
6. **FEATURES benchkit row edit malformed the table** (left a stray trailing `| |` cell): my `old_string` ended mid-row and consumed padding inconsistently. The mandatory post-edit view caught it; fixed in a follow-up. Also left cosmetic padding misalignment on that row (markdown tables don't gate on it; the file's other rows are aligned — small, but visible).
7. **A policy-violating intermediate row:** my first closure of the Doctor-counters TODO row produced a bizarre "[ ] … DONE … REMAINING" chimera — exactly the anti-pattern the skill bans — before I deleted it outright. One wasted edit, zero shipped damage, but the first instinct was wrong.
8. **Mtime-guard rejection on TODO_LIST** (python-script write followed by an edit-tool call without re-viewing) — the documented dance, hit once, cost one round trip.
9. **The inline health report went out before this self-review** — which means the "90-module" error was IN the delivered report's text (the §a6 item said counts "unified to 90"). This report is the correction; §f14's census task exists because of it.

## e) WHAT WE SHOULD IMPROVE (process, from this session)

1. **Extract exact bytes BEFORE composing any scripted edit** — `awk`/`sed -n` the target region first, paste verbatim. Four failures in a row came from summary-composed patterns; zero came from extracted ones. This generalizes the repo's "fresh View before edit" rule to python scripts.
2. **Read an anchor's surrounding ±15 lines before anchoring an insert** — the Code-Quality misfile existed because the anchor text matched while the section context was wrong. Section-membership is part of exact matching.
3. **Count-before-insert for numbered structures** (ROADMAP OQs) and **count-before-claim for any number written into a doc** (90 vs 73). Both failures were "the number felt derivable" — run the command instead.
4. **When two tools disagree, resolve their resolution SEMANTICS before trusting either** (doc-check's doc-name fallback vs check-doc-links' file-relative reality). "Gate X is CI-truth" is not a substitute for understanding what question each gate answers.
5. **Keep the two-tier annotation convention explicit** (strike verified-resolved rows; banner-route brainstorm tails) and either get it ratified into the skill or stop treating it as the letter of "resolve every item."
6. **Partial-read honesty worked and should stay:** the health report said "full or targeted extraction" — that precision is why §b1 is a small finding instead of a trust breach. Keep classifying out loud.
7. **Self-review BEFORE delivering the health report, not after** — §d1 rode in the delivered report because the order was wrong. The 6th pass's "live completion ledger" idea covers this: cross every number/claim in the report against a command run during the pass, before printing it.
8. **Concurrent-session overlap on shared living docs is now the norm, not the exception** — this pass raced another session on SKILL.md/doc-check/TODO_LIST with zero conflicts only because the edits were disjoint. A "living docs write-lock" signal (even a `docs/.lock` convention) would make luck unnecessary (§g1).

## f) Up to 50 things we should get done next (impact-sorted; session-grounded; the canonical backlog already holds the standing items — no duplication)

**From this pass's own tail:**
1. **Census `module-map.md` vs the 90 modules** — 73 rows today; identify the ~17 missing (likely cmd/* + example/* + nested modules), add rows or document the deliberate compaction scope in the map header. My AGENTS line depends on this being true.
2. **Census the FEATURES Module Maturity Matrix vs 90 modules** the same way (queue rows added; whole matrix not counted).
3. **Per-file index for the 2026-09-13..16 wave** in `docs/status/README.md` (wave summary exists; the 09-11 batch got per-file entries — 15+ files here deserve the same).
4. **Add keep-live banners to the two evidence docs** (17-40 T02 verification notes, 15-55 deep-dive): "live EVIDENCE referenced by planning docs — not open work; do not archive by mistake."
5. **Ratify or reject the two-tier annotation convention** (strike-verified + banner-route-brainstorm) — it is the de-facto pass standard since the 6th pass; make it a skill rule or re-strike the tails.
6. **Verify the MariaDB 11.7 VECTOR claim against server source** (ROADMAP now labels it UNVERIFIED; the label is a placeholder until someone reads the actual source tree).
7. **Stamp or soften FEATURES' unverifiable guarantee rows** ("lint passes", "race-free", coverage 86–96%) — last-verified dates or measured refresh (§g3).
8. **README "80+ modules" → "90+ modules"** — still a true lower bound, now selling short by 10.
9. **ROADMAP Release History is hand-maintained and drifted 5 days** — consider an "as of YYYY-MM-DD" stamp per bullet or a generation script from CHANGELOG.
10. **AGENTS' module-map pointer now carries a row count** ("73 of 90") — it will rot with every module add; either accept the rot (it flags the census need) or drop the count after task 1 lands.
11. **Next-pass archive queue:** 18-19, 12-21, 09-17, 08-47, 15-22 become archivable once their remaining routed items resolve — the banners say so explicitly.
12. **Extend the reconcile-a-planning-doc treatment to the 5 classified plans** (to-the-max, publish-reset-v5-train, command-side, OTEL, vector spike) — one verification pass each.
13. **doc-check scan-profile question:** living docs (TODO_LIST/ROADMAP/FEATURES/status README) get only check-doc-links today; evaluate adding them to a doc-check profile for symbol/anchor coverage of the other living docs.
14. **check-doc-links' ">10 live reports" NOTE** — recalibrate the threshold or make it list archivable candidates automatically (26 live reports is the new steady state).
15. **Keep-dir/harness hygiene carry-over** (18-19 §f25): recipes harness failure messages naming snippet headings — still open in TODO.
16. **Migrate this pass's two new Declined entries' wording** into the eventual decision-ADR if the catch-up SLO work ever starts (they are rationale-bearing, not just guards).
17. **Skill-meta suggestion to skill-creator:** the "so what?" test and the anti-dump rule together imply a "report tails live in reports; TODO_LIST holds the routed top" doctrine — worth one line in the docs-health SKILL to stop future passes from re-litigating it.
18. **`git status` hygiene for doc passes:** adopt the concurrent session's improvement — check `git status` at every artifact handoff; this pass noticed the foreign `navlinks.go` change only because the daemon's stat line looked odd.
19. **Consider a `docs/.lock`-style coordination convention** for living-doc passes vs concurrent sessions (§g1) — even a one-line "pass in progress, files touched: …" note in `docs/status/` would do.
20. **Verify the corrected AGENTS formulation** ("73 of 90") is still true after task 1-2's censuses (it is designed to be self-invalidating — good).

**Standing items this session re-confirmed as the right next moves (canonical home = TODO_LIST):** 21. tag wave (otel `DBSystem` re-tag precondition now documented in the wave row) · 22. queue/postgres live-PG conformance leg · 23. config-corruption root cause + depguard auto-restore · 24. pre-commit hook reconciliation · 25. nightly gate cron · 26. zenoh go/no-go decision (ROADMAP OQ #2) · 27. vector verification tail (iroh, system+quickstart blast radius, benchmarks) · 28. NATS roundtrip leg · 29. md-go-validator gate integration · 30. MySQL-VM green shuffled suite · 31. recipes-gate CI posture grep · 32. quiet-window composed `#verify` · 33. `ERRAUDIT_PAT` secret · 34. GitHub Actions billing · 35. cqrs-lint Self-Lint credentials · 36–50: the remaining ~60 open TODO_LIST rows are prioritized there (this pass re-sorted the CI section and refreshed queue/benchkit/vector/watermill sections; the Pareto re-pass post-tag-wave remains the right lens, per the standing Declined note against re-litigating declined items).

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF (max 3)

1. **Concurrent-session coordination on living docs:** this pass overlapped live with another session (SKILL.md/doc-check fixes landed mid-pass; both of us edited shared doc surfaces within minutes of each other, no conflicts — but only by luck of disjoint files). Do you want heavy doc passes serialized (a "pass in progress" marker convention), or is daemon-absorb coexistence the accepted mode going forward?
2. **Annotation-gate conflict, policy ruling:** the skill's completeness gate says every archived file carries `~~` markers; ~1,400 grandfathered pre-September archived files have none, and a standing Declined entry (V3 T42, 2026-08-29 precedent) declines mass-striking them as noise. My pass-scoped interpretation (gate applies to files archived from this pass onward) kept 08-42 — a file with zero forward-looking items — at zero markers. Do you ratify the pass-scoped gate, or do you want the letter enforced (one-time sweep / amended gate script)?
3. **Unverifiable-claim convention for living docs:** FEATURES carries "Lint posture: `nix run .#lint` passes across all modules" and "Race-free" guarantees plus 86–96% coverage claims that I could not re-verify this pass (no composed runs since the 09-16 config recovery). Do you want a "last verified YYYY-MM-DD" stamping convention on such claims (honest, but adds churn every pass), a softening rewrite ("targeted gates green as of <date>"), or leave as-is and treat the quiet-window `#verify` as the single source of truth?

---

**Awaiting instructions.**
