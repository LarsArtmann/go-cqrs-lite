# Status Report: go-finding v1.10.0 → cqrs-lint Benefit Analysis

**Generated:** 2026-09-16 21:05 CEST
**Scope:** This session only — analysis of how `cmd/cqrs-lint` (go-cqrs-lite's linter) could benefit from the latest `github.com/larsartmann/go-finding`. No other subsystems researched, per instruction.
**Skills loaded:** `linter-building` (task), `status-report` + `brutal-self-review` (this report).

---

## Executive Summary

- **cqrs-lint already pins the latest go-finding release** (`v1.10.0` + `pipeline v1.10.0`, released 2026-09-10). Master since then is release-infra chores plus **one unreleased consumer-relevant fix** (`8a9b7c8`: integer-underflow comparator in `correlateByProximity` that silently corrupted proximity correlations).
- The real benefit pool is **8 unadopted capabilities of the version already shipped** — verified against source on both sides, not docs. Top 3 quick wins: `pipeline.Config.OnFixOutcome` (per-finding fix UX, upstream-flagged as L1-36), `finding.ValidateAll` in the ruletest harness, `finding.ParseConfidence` replacing a hand-rolled parser at `filters.go:169`.
- One stale blocker was discovered and annotated: **TODO_LIST.md:369** ("go-finding fetch fails") — verified false today; v1.10.0 resolves via the public module proxy under `GOWORK=off`. The blocking CI leg still needs a re-run.
- **Zero production code changed.** One documentation edit (TODO_LIST.md), verified builds, one delivered analysis.

---

## a) FULLY DONE

| # | Work | Evidence |
|---|------|----------|
| 1 | Loaded `linter-building` skill before any task action; its verified claims (go-finding v1.9.x state, converter-glue history) provided the baseline | SKILL.md read in full |
| 2 | go-finding version state pinned: latest tags = `v1.10.0` (core, pipeline, analysis, cmd/go-finding, toolsdk); `version.go` = 1.10.0; Unreleased = release-workflow hardening + erraudit gate + version-drift fix | `git tag --sort=-creatordate`, CHANGELOG.md |
| 3 | go-finding changelog v1.5.0→v1.10.0 read; consumer-facing capability inventory built (deterministic JSON, ValidateAll, FlightRecorder, ParseConfidence, Template, GroupID, per-file rollback, GroupFindingsSorted, ResolveSafePath, toolsdk) | CHANGELOG.md §1.5–1.10 |
| 4 | Unreleased master diff triaged: only `8a9b7c8` (correlate underflow fix, 2026-09-13) matters to consumers; release-workflow items do not | `git log v1.10.0..HEAD` |
| 5 | cqrs-lint go-finding adoption inventory: ~180 files import go-finding; `finding.Finding` ×526, `Detector` ×222, `NewBuilder` ×171, `FixStrategy` ×78 in prod code; `Report.WriteSARIF`/`PrettyJSON`/`FormatMarkdown` in use; suppression data model stamped via custom `pkg/suppression` parser | `rg` symbol sweeps, `output_grouping.go:70` |
| 6 | **Not-used** list verified: `OnFixOutcome` (`pipeline/config.go:42`), `CorrelateFindings` (`pipeline/config.go:47`), `GroupID`, `Template`, `ParseConfidence`, `ValidateAll`, `analysis` submodule (`AnalyzerDetector`/`FromDiagnostic`), `toolsdk`, FlightRecorder, LSP — all 0 hits in cqrs-lint prod code | per-symbol rg counts |
| 7 | Hand-rolled confidence parser confirmed at `filters.go:168-180` (calls local `parseConfidence`) — direct `finding.ParseConfidence` replacement target | filters.go read |
| 8 | `--fix` outcome gap confirmed: `runPipeline` (run.go:367-388) sets Processors/FixProviders but never `OnFixOutcome`; pipeline consumes `ApplyWithReport` internally and reduces it to metric counts | run.go + `pipeline/pipeline_detect.go:296-320` |
| 9 | Proxy-fetch verification: `GOWORK=off GOPROXY=https://proxy.golang.org go list -m github.com/larsartmann/go-finding@latest` → **v1.10.0** — the TODO_LIST "fetch fails" block is stale | command run in `cmd/cqrs-lint` |
| 10 | Build verification: `cd cmd/cqrs-lint && GOWORK=off go build -tags "goexperiment.jsonv2" ./...` → **BUILD_OK** against proxy-resolved deps | command output |
| 11 | Benefit map (8 items, file:line-cited, effort-estimated) + Pareto-ordered adoption plan delivered to user | chat answer |
| 12 | TODO_LIST.md:369 stale-block annotated with dated verification evidence; edit absorbed by auto-commit daemon (`64d119598`) | TODO_LIST.md diff |
| 13 | Overlapping-rule evidence found for correlation adoption: IMPROVEMENT_IDEAS #25 states C025 exists *because* D006 is general — concrete noise source correlate could address | IMPROVEMENT_IDEAS.md |

## b) PARTIALLY DONE

| # | Work | What remains |
|---|------|--------------|
| 1 | TODO_LIST.md:369 unblock | Annotation done; the actual self-lint CI leg re-run is NOT done (and GitHub Actions billing is separately broken per TODO_LIST.md:366, so re-run may fail for unrelated reasons) |
| 2 | Benefit analysis | Analysis delivered; all 8 recommended adoptions remain unimplemented (they were recommendations, not committed work) |
| 3 | `parseConfidence` replacement target verified | Its full function body was never viewed (call-site + 0 hits for `finding.ParseConfidence` make hand-rolled near-certain, but not eyeballed) |
| 4 | "186 rules" figure | Cited from IMPROVEMENT_IDEAS.md's header, which self-labels as HISTORICAL — not re-counted against RULES.md |

## c) NOT STARTED

All eight adoption items from the benefit map (none were in scope to implement without approval):

1. Wire `OnFixOutcome` into `runPipeline` → per-finding `--fix` reporting (L1-36).
2. `finding.ValidateAll` in `pkg/ruletest.RunDetector`.
3. Swap `filters.go` `parseConfidence` → `finding.ParseConfidence`.
4. First type-fact rule via `go-finding/analysis` `AnalyzerDetector` (pilot).
5. `toolsdk` Spec so cqrs-lint becomes a BuildFlow provider.
6. `GroupID` stamping for multi-site rules (C019-class).
7. `CorrelateFindings` enablement — **gated** on a go-finding release carrying `8a9b7c8`.
8. `finding.Template` sweep across ~171 `NewBuilder` sites.

Also not started (observed, parked): FlightRecorder CLI flags; LSP bridge via `ToLSP`; self-lint CI re-run; go-finding v1.10.1 release cut.

## d) TOTALLY FUCKED UP

Nothing shipped-wrong reached the user, but three process failures are on the record:

1. **Buggy symbol-count loop produced 0-for-everything.** My first feature-usage inventory (shell loop with `-g '!*_test.go'` + awk) printed zero for every symbol including ones provably present (`Suppress`). If trusted, the entire analysis would have been inverted ("linter barely uses go-finding"). Caught only because I cross-checked against the raw import list — the independent-verification rule worked, but the bug class (trusting a homemade pipeline over raw evidence) is exactly what AGENTS.md warns about. Burned a round trip.
2. **Edited before reading.** First `edit` on TODO_LIST.md failed with "you must read the file before editing" — a direct violation of the READ-BEFORE-EDIT rule. Recovered in one step, but it was preventable by following my own checklist.
3. **Re-derived what the skill already contained.** I built the capability inventory from source without reading `linter-building/references/` (`ecosystem.md`, `finding-model.md`) — those references exist precisely to shortcut this and were last verified 2026-09-09 (one day before the release I analyzed). Effort wasted; risk of diverging from house doctrine.

Non-failure but worth stating honestly: all effort estimates (XS/S/M) are judgment, not measurement; and one foreign working-tree change (`metaengine/dgraphengine/engine.go`) appeared and was daemon-absorbed mid-session — I did not author or inspect it (per safety rules, not mine to touch).

## e) WHAT WE SHOULD IMPROVE

1. **Read skill `references/` before deriving from source.** The linter-building skill's reference set is pre-verified doctrine; bypassing it re-does homework and risks drift.
2. **Never trust an ad-hoc analysis loop.** Cross-check any scripted aggregation against raw data (imports, file lists) before reasoning on top of it — the zero-count bug is the recurring lesson (same family as pipeline-masking in AGENTS.md).
3. **View-then-edit without exception**, even for "obvious" single-block replacements.
4. **Verify cited numbers at their source.** The "186 rules" figure traveled from a self-labeled-historical doc into my report; a one-line `rg -c` against RULES.md would have pinned it.
5. **Label estimates as estimates** in deliverables, or calibrate against a measured baseline.
6. **Check the working tree for foreign changes before AND after** multi-step sessions — the daemon + parallel sessions (a `cv-verdicts` status report landed at 21:02 from another session) mean the tree moves under you; state what you saw.

## f) Up to 50 things we should get done next

Ranked roughly by impact-first (Pareto); items 1–8 are the session's core recommendations, 9–50 are session-grounded follow-ups. **Brainstorm, not commitments** — route through `docs-health` HARVEST before entering TODO_LIST.

**Quick wins (this analysis, high impact / tiny effort)**
1. Wire `pipeline.Config.OnFixOutcome` in `run.go` `runPipeline` → print per-finding fix outcomes in `--fix` mode (closes upstream-flagged L1-36 / issue #28 UX).
2. Add `finding.ValidateAll` to `pkg/ruletest.RunDetector` so every rule test fails on invalid findings (fabricated line 0, bad positions).
3. Replace `filters.go` `parseConfidence` with `finding.ParseConfidence`; pin identical behavior with a before/after table test on the `--min-confidence` values.
4. Run `nix run .#verify` + affected-module tests after 1–3 land.

**Analysis unlock (new rule class)**
5. Pilot one type-fact rule as a `go/analysis` analyzer bridged via `go-finding/analysis.AnalyzerDetector` (e.g., interface-satisfaction misuse).
6. Review `go-finding/analysis` addition against `nix run .#check-arch` dep budget for `cmd/cqrs-lint`.
7. Decide which of IMPROVEMENT_IDEAS' parked ideas *require* type facts and queue them behind 5.
8. Document the AST-walker vs go/analysis rule-split convention in cqrs-lint RULES.md/README.

**Grouping & output quality**
9. Audit which rules emit multiple findings per logical problem; stamp `finding.GroupID` (start: C019 multi-repository).
10. Render groups deterministically in text/markdown via `Report.GroupFindingsSorted`.
11. Add SARIF group-property round-trip test in cqrs-lint output tests.
12. Add a byte-identical determinism test for cqrs-lint's SARIF/JSON output (inherits go-finding `json.Deterministic`; pin it so a future regression fails loudly).

**Correlation (blocked until go-finding release)**
13. Cut go-finding release carrying the `8a9b7c8` correlate underflow fix (v1.10.1 or v1.11.0).
14. Then enable `pipeline.CorrelateFindings` behind a cqrs-lint flag; measure FP/noise delta on a consumer corpus.
15. Add a regression pin for the underflow class: correlate ordering test with sentinel line values (0 / negative-relative) before+after upgrade.
16. Sweep cqrs-lint for subtraction-style sort comparators (`a.Line - b.Line`) — same bug class as `8a9b7c8`; adopt `cmp.Compare`.
17. Surface `PipelineResult.Correlations` in the `doctor` command for correlation-aware triage.

**BuildFlow / toolsdk integration**
18. Author a `toolsdk.Spec` for cqrs-lint (Detect; Repair only for safe-fixable rules).
19. Pilot cqrs-lint as a BuildFlow provider in one power-user consumer repo.
20. Document the BuildFlow wiring recipe in cqrs-lint README.

**Template & rule consistency**
21. Introduce per-rule `finding.Template` for the rule categories (mechanical sweep, one category per PR).
22. During the sweep, standardize severity/confidence defaults in rule meta (single source of truth).
23. Delete now-dead builder boilerplate helpers, if any, after the sweep.

**Suppression alignment (split-brain risk noticed)**
24. Cross-check `pkg/suppression` parser output against go-finding's suppression validation rules (two validation stacks for one data model).
25. Map cqrs-lint suppression directives to go-finding's `in-review` kind where applicable.
26. Evaluate go-finding suppression expiry vs `pkg/suppression/stale.go` — converge or document why divergent.

**CI / release hygiene**
27. Fix GitHub Actions billing (user action; blocks everything below).
28. Re-run the cqrs-lint self-lint CI leg (proxy fetch now verified) and close TODO_LIST:369.
29. Add go-finding version to `cqrs-lint --version` output (dependency transparency).
30. Add go-finding to the pin-sweep inventory; subscribe to its releases.
31. Weekly canary: run cqrs-lint tests against go-finding master to catch breaking changes early.

**Docs truthfulness**
32. Re-count and reconcile the "186 rules" claim between IMPROVEMENT_IDEAS.md and RULES.md (single source of truth).
33. Refresh IMPROVEMENT_IDEAS.md header (self-labeled HISTORICAL; counts stale).
34. Document `--fix` outcome reporting (once item 1 lands) in README + RULES.md.
35. Run `cmd/doc-check` explicitly over TODO_LIST.md after doc edits (outside its default scan set).

**Pipeline config scrutiny (noticed this session)**
36. Justify `MaxIterations: 5` with evidence or tune it.
37. Document `GracefulDegradation` / `Timeout: 5m` choices in cqrs-lint docs.
38. Evaluate `pipeline.ConfigFile` vs cqrs-lint's koanf config for parity drift.
39. Verify `--fix` path safety inherits `pipeline.ResolveSafePath` given `cfg.Path` as root; add a traversal test.
40. Add a detector-timing regression gate using `pipeline.MetricsSnapshot` (currently stderr-only via `printDetectorTimings`).
41. Expose pipeline FlightRecorder behind a `--trace` flag for slow-detector diagnosis.

**Editor / ecosystem (parked)**
42. Spike an LSP bridge using go-finding `ToLSP` (cqrs-lint findings in editors).
43. Add `FromLSP` round-trip tests if 42 proceeds.
44. Add per-rule confidence/severity table to RULES.md so `--min-confidence` is meaningful to users.
45. Measure top-N rule FP rates on the 45-consumer corpus before/after correlation (skill doctrine: corpus-validated rules).

**Testing doctrine**
46. Add a discrimination-proof test: a deliberately broken mutant rule must fail the suite (proves the tests test).
47. Verify `finding.Confidence` ordering contract (`>=` in `filterByConfidence`) against go-finding docs before relying on cross-version semantics.
48. Ask go-finding (own repo) to expose per-finding `FixOutcome`s directly on `PipelineResult` — today it's callback (`OnFixOutcome`) or metric counts only; a `result.FixOutcomes()` accessor would simplify item 1.
49. After adoption lands, update `.agents/skills/go-cqrs-lite` references + SKILL.md and run doc-check (procedure: "Change an Exported Symbol").
50. Re-verify this report's status claims against source before acting on them (reports are point-in-time).

## g) Questions I cannot figure out myself

1. **Implement now or analyze-only?** Should I implement the three quick wins (items 1–3: `OnFixOutcome` wiring, `ValidateAll` in ruletest, `ParseConfidence` swap) right away, or does this stay analysis-only until you approve the adoption plan?
2. **BuildFlow ownership:** is cqrs-lint-as-`toolsdk`-provider (items 18–20) meant to be built in cqrs-lint, in BuildFlow, or both — and is it a current priority or parked?
3. **go-finding release authority:** shall I cut the go-finding release that carries the correlate underflow fix (`8a9b7c8`) so correlation adoption unblocks, or is go-finding's release cadence entirely yours to drive?

---

*Report per `status-report` skill. Format note: skill default is a styled HTML dashboard; the explicit `.md` instruction in the request was honored (override flagged per skill contract). No manual commit made (Crush harness forbids commits without explicit request) — the auto-commit daemon absorbs this file.*
