# Session 27 — Comprehensive Codebase Audit & Fixes

**Date:** 2026-05-01

## Summary

Resumed from interrupted session 25-26. Performed comprehensive codebase audit identifying 20+ issues across 8 modules. Fixed critical items, deferred breaking changes for separate review.

## Changes Made

### Bug Fixes

| Change                                    | Module     | Detail                                                                                          |
| ----------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------- |
| `catalog/go.mod` stale replace directives | catalog    | Removed `memory` and `testhelpers` from replace (not in require). Fixed 33+ gopls errors        |
| False-positive `TestSQLEventStore_Close`  | storage    | Removed `mock.ExpectClose()` — `Close()` is a no-op, so the expectation was never fulfilled     |
| `event.Bus` missing `Use()`               | core/event | Added `Use(middleware ...Middleware) error` to `Bus` interface. Updated `FakeBus` and `stubBus` |

### Breaking Changes

| Change                             | Module         | Detail                                                                                                        |
| ---------------------------------- | -------------- | ------------------------------------------------------------------------------------------------------------- |
| `NewInMemoryRunner` returns error  | core/event     | `(*InMemoryRunner)` → `(*InMemoryRunner, error)`. Replaces `panic` with `ErrNilCheckpointStore` sentinel      |
| `NewCore` validates inputs         | core/aggregate | `*Core` → `(*Core, error)`. Returns `ErrNilAggregateID` / `ErrEmptyAggregateType`. Added `MustNewCore` helper |
| `NewOutboxPublisher` returns error | core/event     | Panic → error return (done by pre-commit hook auto-fix)                                                       |

### Documentation

| Change                                              | File        | Detail                                                             |
| --------------------------------------------------- | ----------- | ------------------------------------------------------------------ |
| Remove implemented items from "Not Yet Implemented" | FEATURES.md | SQL SnapshotStore, CheckpointStore, Outbox publisher already exist |
| Fix Bus.Use claim                                   | FEATURES.md | `Use` was on `MemoryBus` only, now on interface                    |
| Fix Close() description                             | FEATURES.md | No-op, not wrapping `db.Close()`                                   |
| Fix SQLEventStoreOption reference                   | FEATURES.md | Type doesn't exist                                                 |

### Code Quality

| Change                     | Module       | Detail                                                                       |
| -------------------------- | ------------ | ---------------------------------------------------------------------------- |
| Unexport `Exporter` fields | catalog/d2   | Use `WithDescription`/`WithDirection` options instead of direct field access |
| Simplify `drawn` counter   | catalog/d2   | Changed `int` counter to `hasCrossService bool`                              |
| Fix example `ChangeName`   | example/user | Removed `u.name = name` — now relies on `Apply` pattern                      |

## Items Deferred (Need Separate Decision)

| Item                                                       | Reason                                                                                |
| ---------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| `Load()` empty semantics (memory → err, storage → nil)     | Breaking API change for consumers; integration tests depend on `ErrAggregateNotFound` |
| `CatalogMeta` deduplication across 3 packages              | Extract to shared `core/pkg/catalog` — needs design discussion                        |
| `query.Handler` returns `any`                              | Project rule "no any types" but generic return types have performance implications    |
| Error wrapping consistency (`fmt.Errorf` vs `errors.Wrap`) | Low impact, high churn                                                                |

## Test Results

All 18 packages pass:

```
ok  core/aggregate    core/command    core/event    core/pkg/dispatcher
ok  core/pkg/id       core/query      memory        catalog
ok  catalog/adapters  catalog/asyncapi  catalog/d2  catalog/eventcatalog
ok  middleware        integration/aggregate  integration/command  integration/event
ok  integration/query storage
```

Zero lint issues.

## Architecture Findings (Not Fixed)

| Finding                                                        | Severity | Detail                                                                        |
| -------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------- |
| `Root.LoadEvents` vs `Core.LoadFromHistory` interface mismatch | MEDIUM   | Every aggregate must implement `LoadEvents` and delegate to `LoadFromHistory` |
| Catalog types all use raw `string` (no branded types)          | MEDIUM   | `ServiceID`, `MessageName`, `SemVersion` could be branded                     |
| `OutboxID` has no methods or validation                        | LOW      | Just `type OutboxID string`                                                   |
| `Snapshot.State` is `[]byte` with no encoding marker           | LOW      | JSON vs raw bytes indistinguishable                                           |
| Middleware triplication                                        | LOW      | Every middleware implemented 3× (Command/Event/Query) — could use generics    |
