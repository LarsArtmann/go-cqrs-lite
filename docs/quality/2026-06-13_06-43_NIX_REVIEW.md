# Nix Review Report — 2026-06-13

## Files Reviewed

- `flake.nix` (173 lines)

## Executive Summary

| Metric                   | Count |
| ------------------------ | ----- |
| Critical issues          | 0     |
| Structural issues        | 1     |
| Consistency issues       | 3     |
| Best practice violations | 2     |
| Strengths                | 8     |

## Critical Issues

None. Flake builds and works correctly.

## Structural Issues

| #   | Issue                                         | Recommendation                                                                                                                                                   |
| --- | --------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `flake.nix` is 173 lines with all apps inline | Consider extracting app definitions to a separate file or using flake-parts modules for cleaner organization. Acceptable at this size but approaching the limit. |

## Consistency Issues

| #   | Issue                                                      | Recommendation                                                                                                                                                                                                  |
| --- | ---------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | DevShell uses `pkgs.mkShell` instead of `pkgs.mkShellNoCC` | The devShell only needs Go tooling — no C compiler needed. Use `mkShellNoCC` for faster evaluation. However, Go CGO may need gcc for some deps (e.g., sqlite3 for turso/storage tests). Verify before changing. |
| 2   | No CI-specific devShell defined                            | Consider adding a `devShells.ci` using `mkShellNoCC` with minimal packages for CI (go, golangci-lint only).                                                                                                     |
| 3   | `checks.build` not defined — only `checks.format` exists   | Add `checks.build = config.apps.build;` or similar to get build verification in `nix flake check`.                                                                                                              |

## Best Practice Violations

| #   | Issue                                                                                   | Recommendation                                                                                                                     |
| --- | --------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `GOWORK = "off"` as env var attribute is correct, but devShell doesn't include `d2` CLI | Project generates D2 diagrams but devShell doesn't include `d2`. Add `pkgs.d2` to devShell packages.                               |
| 2   | `shellHook` uses `echo` which creates noise in non-interactive contexts                 | The `echo "go-cqrs-lite dev shell — $(go version)"` is acceptable but could be gated with `[[ -t 1 ]]` for non-interactive safety. |

## Strengths

- Standard stack: `flake-parts` + `treefmt-nix` + `systems` — exactly the recommended pattern
- Go version properly pinned: `pkgs.go_1_26` in `let` binding
- `follows` correctly used: `nixpkgs-lib.follows = "nixpkgs"`, `nixpkgs.follows = "nixpkgs"`
- `mkApp` helper reduces boilerplate — clean pattern
- `writeShellApplication` for all apps — proper sandboxing
- treefmt configured with `gofumpt`, `goimports`, `golines`, `nixfmt` — comprehensive formatting
- Golden test files excluded from formatting via `settings.excludes`
- No `rec` attrsets, no `with` at top level, no `<nixpkgs>` lookups
- No secrets, no impure constructs, no placeholder hashes
