## branching-flow all

Run all linters and show combined results

### Synopsis

all runs all branching-flow linters in the AllSet on a codebase
and displays combined findings across all analysis types.

This provides a comprehensive analysis by running:

- context: Analyze semantic context loss in error handling
- dupe: Find duplicate or similar interface/struct definitions
- phantom: Analyze phantom type opportunities
- panic: Analyze potential panic conditions
- strong-id: Find parameters with 'id' in the name that should use strong ID types
- boolblind: Analyze structs with multiple bools that should be bit flags
- anti-patterns: Analyze O(n) structural anti-patterns
- mixins: Analyze O(n²) mixin composition opportunities

Each linter runs independently and results are presented together.

Examples:
branching-flow all ./src
branching-flow all --verbose ./src

```
branching-flow all <path> [flags]
```

### Options

```
      --exclude-generated                   Exclude generated files (default true)
      --exit-code                           Exit with code 1 if issues found
  -h, --help                                help for all
      --verbose verbosity level[=Verbose]   Verbosity level (Silent, Normal, Verbose) (default Normal)
```

### SEE ALSO

- [branching-flow](branching-flow.md) - Analyze Go code for semantic context loss in error handling

###### Auto generated by spf13/cobra on 30-Mar-2026
