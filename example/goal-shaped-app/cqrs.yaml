# Operator config — the ONLY file you touch to change where data lives.
#
# Swap to postgres without touching domain code (both drivers ship in the
# binary; see the blank imports in main.go):
#
#   engines:
#     primary:
#       driver: postgres
#       dsn: postgres://user:pass@localhost:5432/goal
#
# or leave the file alone and override per environment:
#
#   CQRS_ENGINES__PRIMARY__DRIVER=postgres
#   CQRS_ENGINES__PRIMARY__DSN=postgres://user:pass@localhost:5432/goal
engines:
  primary:
    driver: sqlite
    dsn: goal.db
    pragmas: ["journal_mode=WAL", "busy_timeout=5000"]
    # The "task stats" DATA PRODUCT's output port is an OPERATOR-declared
    # materialized view (ADR-0135 — developers never declare views). IVM
    # views need libSQL's experimental views feature, so on the default
    # sqlite driver this block is the documented upgrade path, not active:
    # uncomment AND switch to the turso driver (libSQL) to serve it:
    #
    #   driver: turso            # + blank-import metaengine/tursoengine
    #   dsn: goal.db
    #   materialized_views:
    #     - collection: tasks
    #       fn: COUNT
    #
    # Scalar COUNT views are exact; grouped views carry an upstream
    # correctness caveat (readmodels.md §Materialized-view acceleration).
instances:
  - role: source-of-truth
    engine: primary
  - role: projections
    engine: primary
