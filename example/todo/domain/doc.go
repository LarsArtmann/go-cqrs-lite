// Package domain defines the core domain model and shared vocabulary for the
// todo aggregate example: the TodoID branded identifier, the Todo read model,
// status and priority value objects, a TodoFilter, and the TodoReadModel
// interface that projections implement and queries consume. It holds no
// business logic beyond validation and clone helpers.
package domain
